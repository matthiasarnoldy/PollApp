/**
 * Represents the lifecycle status of a survey.
 * - `draft`: The survey has been saved but not yet published.
 * - `published`: The survey is live and visible to participants.
 */
export type SurveyStatus = 'draft' | 'published';
